<h1>Hotels</h1>
  <% @hotels.each_with_index do |hotel, index| %>
    <div id="hotel-<%= index %>">
    <h3><%= hotel.name %> - <time> Created at: <%= hotel.created_at %></time></h3>
    <!-- <p>City: <%= hotel.city %></p>
    <p>Booked: <%= hotel.booked %></p>
    <p>Capacity: <%= hotel.capacity %></p> -->
    </div>
  <% end %>
